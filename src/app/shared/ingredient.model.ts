export class Ingredient {

	// long version of model
	// public name: string;
	// public amount: number;

	// constructor(name: string, amout: number) {
	// 	this.name = name;
	// 	this.amout = amout;
	// }

	// short version of model
	constructor(public name: string, public amount: number) {}
}